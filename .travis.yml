sudo: required

language: ruby

services:
  - docker

before_install:
  - docker build -t lagoonplatform/ansible-docker-alpine:latest .

deploy:
  provider: script
  script: bash docker_push
  on:
    branch: Fix_AWS_endpoint